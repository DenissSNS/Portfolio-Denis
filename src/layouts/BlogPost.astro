---
import BaseHead from '../components/BaseHead.astro';

import Header from '../components/navbar/Header.astro';
import Footer from '../components/footer/Footer.astro';
import Retour from '../components/footer/Retour.astro';

import DCard from '../components/inArticle/DCard.astro';
import BottomArticle from '../components/inArticle/BottomArticle.astro';

import "./blogPost.scss"

const { title, description, pubDate, heroImage, tags } = Astro.props;

import { getLangFromUrl } from '../i18n/utils';
const lang = getLangFromUrl(Astro.url);

---

<!-- template des projets -->
<html lang={lang}>
	<head>
		<BaseHead title={title} description={description} />
	</head>

	<body>
		<Header />
		
		<main>
			<article>
				<Retour /> 

				<div class="hero-image">
					{heroImage && <img width={1260} height={469} src={heroImage} alt="main img" />}
				</div>

				<div class="infos">
					
					<div class="all-txt">
						<div class="main-txt">
							<!-- article en lui-même  -->
							<slot />
							
						</div>
						<BottomArticle />
					</div>

					<DCard />
				</div>

				<Footer />
			</article>
		</main>
	</body>
</html>
